{{- if .Values.istio.enabled }}
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: {{ .Chart.Name }}
  namespace: {{ .Values.namespace | default .Release.Namespace }}
  labels:
    {{- range $key, $value := .Values.labels }}
    {{ $key }}: {{ $value | quote }}
    {{- end }}
spec:
  hosts:
    {{- if .Values.istio.preview.enabled }}
    - {{ .Values.istio.preview.hostname }}
    {{- else }}
    - {{ .Chart.Name }}.{{ .Values.istio.domain }}
    {{- end }}
  gateways:
    - {{ .Values.istio.gateway }}
    {{- if .Values.istio.preview.enabled }}
    - istio-system/cross-network-gateway  # East-west gateway for cross-cluster routing
    {{- end }}
  http:
    - match:
        - uri:
            prefix: /
      route:
        - destination:
            host: {{ .Chart.Name }}
            port:
              number: {{ .Values.service.port }}
{{- end }}
